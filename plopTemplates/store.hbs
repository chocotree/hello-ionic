import { defineStore } from 'pinia';

const use{{pascalCase name}} = defineStore('{{ camelCase name }}', () => {
  return {
    //
  };
});

export const use{{pascalCase name}}Store = () => ({ {{ camelCase name }}Store: use{{pascalCase name}}() });
